import React from 'react';

import {formatRuntime} from '../../utils/format-time';
import FilmProp from '../film/film.prop';

function FilmDetails({film}) {
  const {starring, director, runTime, genre, released} = film;
  const stars = starring.join(', :').split(' :').map((star, index) =>
    (<React.Fragment key={star}>{star}{index !== starring.length - 1 && (<br/>)}</React.Fragment>));

  return (
    <div className="film-card__text film-card__row">
      <div className="film-card__text-col">
        <p className="film-card__details-item">
          <strong className="film-card__details-name">Director</strong>
          <span className="film-card__details-value">{director}</span>
        </p>
        <p className="film-card__details-item">
          <strong className="film-card__details-name">Starring</strong>
          <span className="film-card__details-value">
            {stars}
          </span>
        </p>
      </div>
      <div className="film-card__text-col">
        <p className="film-card__details-item">
          <strong className="film-card__details-name">Run Time</strong>
          <span className="film-card__details-value">{formatRuntime(runTime)}</span>
        </p>
        <p className="film-card__details-item">
          <strong className="film-card__details-name">Genre</strong>
          <span className="film-card__details-value">{genre}</span>
        </p>
        <p className="film-card__details-item">
          <strong className="film-card__details-name">Released</strong>
          <span className="film-card__details-value">{released}</span>
        </p>
      </div>
    </div>
  );
}

FilmDetails.propTypes = {
  film: FilmProp,
};

export default FilmDetails;
